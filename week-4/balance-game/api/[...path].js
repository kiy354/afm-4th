// ============================================================
// Vercel 서버리스 진입점 (/api/* 전체를 받는 catch-all)
// ------------------------------------------------------------
// server.js 가 내보낸 요청 핸들러를 그대로 재사용한다.
// 서버리스에서는 플랫폼이 요청마다 핸들러를 호출하므로 listen 은 하지 않는다.
//
// 파일명이 [...path] 인 이유: rewrite 로 한 파일에 몰아주면 req.url 이
// 덮여서 server.js 라우터가 실제 경로(/api/questions/3/vote)를 못 본다.
// catch-all 로 받으면 원본 경로와 메서드가 그대로 유지된다.
//
// 정적 파일(public/)은 Vercel CDN 이 직접 서빙하므로 여기까지 오지 않는다.
// ============================================================

module.exports = require('../server.js');
